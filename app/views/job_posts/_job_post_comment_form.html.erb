<%= form_for(JobPostComment.new) do |f| %>
  <div><%= hidden_field_tag :user_id, current_user %></div>
  <div class="field">
    <%= f.text_area :comment, placeholder: "Comment..." %>
  </div>
  <%= f.submit "Post", class: "btn btn-primary" %>
<% end %>
